events {}
http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  
  server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Rota do front
    location / {
      try_files $uri $uri/ /index.html;
    }

    location /health_front {
      return 200 "OK";
    }
  }
}
